#pragma once

#define STATIC_CAST_BEGIN(to) static_cast<to> (
#define STATIC_CAST_END )

#define REINTERP_CAST_BEGIN(to) reinterpret_cast<to> (
#define REINTERP_CAST_END )

#define DYNAMIC_CAST_BEGIN(to) dynamic_cast<to> (
#define DYNAMIC_CAST_END )

